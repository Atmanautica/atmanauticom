{
  "private": true,
  "name": "atmanauticom",
  "version": "0.0.0",
  "license": "CC-BY-NC-SA-4.0",
  "scripts": {
    "build": "lerna run build --parallel",
    "build-com": "lerna bootstrap && (cd com && npm run build)",
    "build-cms": "lerna bootstrap && (cd cms && npm run build)",
    "dev": "lerna run dev --parallel",
    "format": "lerna run format",
    "graphql-deploy": "lerna run graphql-deploy",
    "lint": "lerna run lint",
    "postinstall": "lerna bootstrap",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@sanity/cli": "^1.149.18",
    "eslint": "^7.5.0",
    "lerna": "^3.22.1"
  },
  "dependencies": {}
}
